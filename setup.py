from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='build ml project to predict swiggy delivery time',
    author='Pranay Gundu',
    license='',
)
